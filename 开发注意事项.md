# TierNest 开发与排障注意事项

> 隐私说明：本文节点名、网络地址、端点、MAC 和位置均已替换为示例值，不用于连接真实设备。历史记录用于解释故障机制，当前升级策略以 AGENTS.md 为准。

> **2026-09-12 / v1.0.2 起的新规则**：按用户要求，只发布一个 arm64 通用安装包，覆盖升级自动继承本机配置。不得再构建三个机型/私密版本，不得用 profile_revision、hostname 或 IP 不匹配覆盖旧配置。旧规则仅作历史参考，当前维护规则以 `AGENTS.md` 为准。

> 历次真机故障、路由模式回归、恢复逻辑、热点转发和发布检查清单统一维护在 [`TierNest-故障与踩坑记录.md`](./TierNest-故障与踩坑记录.md)。新增设备问题修复后必须同步更新该文档。


> 最后更新：2026-09-06  
> 项目目录：`<workspace>/TierNest`

本文档记录 TierNest 在 Android、KernelSU、Magisk、EasyTier、Android VPN 和策略路由适配过程中已经确认的经验，避免后续版本重复引入相同问题。

## 1. 基本原则

1. **不要只根据“节点已显示”判断网络可用。**
   - 节点发现、EasyTier 控制面、ICMP、TCP、Android 应用流量是不同层次。
   - 拓扑中出现节点，只能说明 EasyTier 路由信息存在，不代表浏览器、SSH 或子网 TCP 一定可用。

2. **不要只根据 Ping 成功判断修复完成。**
   - 必须同时检查：
     - `10.42.0.1` Ping；
     - `192.168.50.1` Ping；
     - `192.168.50.1:80` TCP；
     - 浏览器或普通 Android 应用的实际访问。

3. **先确认流量属于哪个 EasyTier 实例。**
   - TierNest Root 模块；
   - EasyTier 官方 APP 的 Android `VpnService`；
   - OpenWrt EasyTier；
   - 随身 WiFi EasyTier。
   - 多个实例使用相同虚拟 IP 时，日志和探测结果很容易被误判。

4. **诊断必须按照时间线判断因果关系。**
   - 某个 APP/VPN 出现在故障之后，可能是用户的临时替代方案，而不是最初故障原因。
   - 不要把“故障后的操作”错误判断成“故障起因”。

---

## 2. 已确认的设备默认配置

### 2.1 通用 Android 稳定配置

```toml
[flags]
dev_name = "tiernest0"
enable_kcp_proxy = false
use_smoltcp = false
```

说明：

- 正式设备包统一使用 `tiernest0`，便于日志、VPN 检测和卸载清理保持一致；
- WebUI 仍允许用户将 `dev_name` 留空，让 EasyTier 自动创建 `tun0/tun1`；
- 留空必须原样保存，不能被 WebUI 自动改写为 `tiernest0`。

### 2.2 小米 / HyperOS

已确认以下组合可能出现：

```toml
enable_kcp_proxy = true
use_smoltcp = false
```

故障特征：

- 节点发现正常；
- 路由正常；
- Ping 正常；
- HTTP、SSH 等 TCP 超时。

稳定默认值：

```toml
enable_kcp_proxy = false
use_smoltcp = false
```

如果确实需要 KCP，可尝试：

```toml
enable_kcp_proxy = true
use_smoltcp = true
```

但需要提示用户会增加少量 CPU 和内存开销。

### 2.3 一加 Ace 3 Pro

**不要再强制固定 `ROUTE_MODE=dedicated`。**

beta.12/beta.13 曾错误地固定：

```text
ROUTE_MODE=dedicated
ROUTE_TABLE=20110
ROUTE_RULE_PRIORITY=9980
```

实际日志出现：

```text
Reconciled 3 route(s), removed=1
```

并且连通性不断在 `OK/FAIL` 之间变化，说明独立表发生路由震荡。

一加应恢复：

```text
ROUTE_MODE=auto
ROUTE_AUTO_SWITCH_ENABLED=1
```

让模块根据实际探测在 `upstream`、`target-main` 和 `dedicated` 之间选择，而不是按机型武断固定。

---

## 3. KCP 与 smoltcp 的判断边界

1. **KCP 只影响 EasyTier 内部 TCP 包装路径。**
   - 如果 EasyTier 连公共节点都没有成功；
   - 路由列表只有本机；
   - 传输 socket 仍处于 `SYN_SENT`；
   - 此时开关 KCP 不会解决问题。

2. **`use_smoltcp` 不会接管手机全部 TCP。**
   - 它主要影响 EasyTier 的代理、KCP 和相关 TCP 处理路径。

3. **排障顺序：**
   1. 是否存在远端 EasyTier 节点；
   2. 是否存在有效传输连接；
   3. TUN 与路由是否正确；
   4. 强制 TUN Ping 是否正常；
   5. 普通应用路由是否正常；
   6. 最后再判断 KCP/smoltcp。

---

## 4. 健康检查与自动恢复

### 4.1 不要在“无远端节点”时反复重启核心

EasyTier 路由列表只有本机时，通常表示正在重连公共节点。EasyTier 自身具有重连机制。

错误行为：

```text
连接尚未建立
→ 健康检查失败
→ 强制重启 EasyTier
→ 重新开始连接
→ 再次失败
```

正确行为：

- `remote_peer_count=0` 时抑制健康重启；
- 记录 `transport-reconnect-wait`；
- 允许 EasyTier 自己持续重连；
- 只有已经存在远端节点、数据面仍异常时，才考虑重启核心。

### 4.2 避免 Shell 全局变量污染

Android `/system/bin/sh` 和 BusyBox ash 的函数变量默认可能影响外层函数。

不要使用过于通用的变量名：

```sh
reason=
pid=
status=
```

恢复函数应使用带前缀的变量：

```sh
restart_reason=
restart_started=
restart_finished=
restart_duration=
```

否则可能产生错误日志：

```text
after-restart-before-restart-health-overlay-unreachable
```

### 4.3 健康探测不能破坏稳定连接

- TCP 探测只用于记录和辅助判断；
- 目标服务端口关闭不能触发无限重启；
- 公网 `1.1.1.1` Ping 失败不能单独判定底层网络断开；
- 某些运营商或 Wi-Fi 会屏蔽 ICMP。

---

## 5. EasyTier APP 与 TierNest 冲突判断

EasyTier 官方 APP 包名/信号示例：

```text
com.kkrainbow.easytier
TauriVpnService
VPN:com.kkrainbow.easytier
```

判断时必须同时检查：

```text
core_pid
tun
config_dev_name
Android VPN owner
VPN sessionId
```

典型状态：

```text
core_pid=stopped
config_dev_name=tiernest0
tun=tun0
VPN:com.kkrainbow.easytier
```

这说明：

- TierNest 核心没有运行；
- `tun0` 属于官方 APP；
- 不能把 APP 的 `tun0` 当成 TierNest 的 TUN；
- APP 和模块使用同一个虚拟 IP 时必须明确提示冲突。

但是要注意：

> 用户可能因为 TierNest 已经故障才打开 APP，APP 是结果，不一定是最初原因。

模块应做到：

- 核心停止时不再通过虚拟 IP兜底识别其他程序的 TUN；
- 检测到 EasyTier APP VPN 时显示明确冲突提示；
- 不自动强制停止用户 APP；
- 不在拓扑中继续显示已经停止的 TierNest 缓存数据。

---

## 6. 拓扑图数据规则

### 6.1 节点分类

- 本机：`path_len=0` 或 `next_hop_hostname=Local`；
- 虚拟节点：`ipv4` 非空；
- 公共服务器：`ipv4` 为空或 hostname 以 `PublicServer` 开头；
- 子网网关：虚拟节点且存在 `proxy_cidrs`；
- 代理网段：挂载在对应网关下的派生节点。

### 6.2 连线规则

- `DIRECT`：本机到目标节点的实线；
- 下一跳为公共服务器：
  - 显示服务器时：本机 → PublicServer → 目标；
  - 隐藏服务器时：折叠为本机 → 目标的虚线，详情保留服务器名称；
- 下一跳为其他虚拟节点：下一跳虚拟节点 → 目标；
- 下一跳不存在且 `path_len>1`：标记为未知/其他下一跳，不能显示为直连。

### 6.3 不要截断虚拟节点

不能再使用：

```js
slice(0, 28)
```

必须保留：

- 所有虚拟节点；
- 这些虚拟节点依赖的下一跳服务器；
- 全部虚拟节点详情卡片。

### 6.4 拓扑数据只代表当前在线状态

`easytier-cli route list` 只反映当前路由/节点：

- 离线节点不能伪装成在线；
- 可以保留离线节点的位置配置；
- 核心停止时应清空当前连接图。

---

## 7. 地理位置与 IP 粗略定位

### 7.1 不能使用虚拟 IP定位

以下地址属于私有虚拟网段：

```text
10.42.0.1
10.42.0.10
10.42.0.51
10.42.0.52
```

它们不能用于现实地理定位。

### 7.2 自动定位必须使用公网 IP

正确方向：

1. 每个 TierNest 节点获取自己的公网 IPv4/IPv6；
2. 根据公网 IP查询省市级粗略位置；
3. 标记为：

```text
示例城市（IP 粗略定位）
```

4. 节点自行上报位置元数据；
5. 手动位置只作为覆盖项。

注意：EasyTier 当前的 `route list`、`peer list` 和 `peer-center` 并不直接提供其他虚拟节点的公网 IP，因此不能从本机凭空推导所有对端位置。必须设计节点自上报或共享元数据机制。

### 7.3 位置可信度标识

建议区分：

- `精确配置`：用户填写的经纬度；
- `IP 粗略定位`：公网 IP查询结果；
- `名称推断`：仅公共服务器根据名称推断；
- `未设置位置`：没有可靠数据。

禁止把名称推断或 `0,0` 显示成精确位置。

---

## 8. WebUI 配置保存

1. 空 `dev_name` 必须保持为空；
2. 未展示的 TOML 高级字段必须无损保留；
3. 位置保存必须：
   - Base64 传输；
   - 后端校验；
   - 经度范围 `-180..180`；
   - 纬度范围 `-90..90`；
   - 原子替换；
   - 保存前备份；
   - 失败时不修改前端内存状态、不关闭编辑窗口；
4. hostname 和 location 不允许包含 `|`、回车或换行。

---

## 9. 升级与设备 Profile

1. 增加 `profile_revision` 前必须明确目的；
2. 不要仅因为普通 UI更新就强制覆盖设备配置；
3. 强制迁移配置时必须保留：
   - 升级前 `config.toml`；
   - `node-locations.conf`；
   - 热点状态；
   - 原有备份；
4. 用户从旧版本跨版本升级时，要对比跨过的所有网络行为变化，不能只比较相邻版本。

---

## 10. 发布前测试清单

### 网络测试

- [ ] 一加 Ace 3 Pro：Wi-Fi；
- [ ] 一加 Ace 3 Pro：移动数据；
- [ ] 小米平板 Android 16；
- [ ] 小米 10 Android 13 / Magisk；
- [ ] Clash/其他 VPN 开启和关闭；
- [ ] 回到家庭 LAN 同网段；
- [ ] 离开家庭 LAN；
- [ ] 热点共享；
- [ ] EasyTier APP VPN 冲突提示。

### 连通性测试

- [ ] 虚拟 IP Ping；
- [ ] 代理网段 Ping；
- [ ] HTTP/TCP；
- [ ] 普通 Android 应用访问；
- [ ] 路由表稳定，不能持续出现无意义 `removed=1`；
- [ ] 无远端节点时不重启风暴。

### WebUI 测试

- [ ] 412px 移动端不横向溢出；
- [ ] 显示全部 / 仅虚拟节点；
- [ ] DIRECT 实线；
- [ ] 公网中继折叠虚线；
- [ ] 虚拟下一跳连线；
- [ ] 全部虚拟节点详情；
- [ ] 地图/位置编辑入口不再出现，旧位置配置不影响路由树；
- [ ] 多跳、缺失下一跳和折叠中继显示虚线，不伪造直连；
- [ ] 占位节点不计入已上报节点数量，所有代理网段连线均有实际端点；
- [ ] 核心停止后不显示缓存在线拓扑。

### 安全与发布

- [ ] 全部 Shell 测试；
- [ ] WebUI 浏览器集成测试；
- [ ] `npm audit`；
- [ ] ZIP 完整性；
- [ ] SHA256；
- [ ] source.zip 不含 `.private`、密钥和设备私人配置；
- [ ] 私密包不得公开分享。

---

## 11. 排障时优先读取的日志字段

```text
module_version
core_pid
config_mode
config_dev_name
tun
external_vpn
Android VPN owner
route_mode_active
route_table
route_rule_priority
remote_peer_count
config_enable_kcp_proxy
config_use_smoltcp
tcp_compatibility
health_restart_count
last_health_event
last_recovery_event
```

同时检查：

```text
ip -4 rule
ip -4 route show table main
ip -4 route show table 20110
ip route get 10.42.0.1
ip route get 192.168.50.1
easytier-cli route list
easytier-cli peer list
easytier-cli peer-center
```

必须确认探测流量实际经过哪个 TUN 和哪个 EasyTier 实例。


## v1.0.0 可靠性约束（2026-09-06）

1. 网络事件必须分开“观察到”和“已经恢复”；冷却、防抖失败及重启失败不能消费待处理事件。
2. 配置备份与原子保存必须共用写锁。Shell 子进程不能直接用 `$$` 冒充实际锁主 PID；使用 shell 内建 `read` 打开 `/proc/self/stat` 取得当前进程。无 owner 文件可能是创建窗口，不得擅自判作死锁删除。
3. UI 的 `overview_sample` 共享仅在单次概览命令的子 Shell 中有效。不要将缓存结果用于守护进程健康恢复。
4. 所有异步读取要防重复、过期返回；隐藏页面要失效旧会话，服务变更后要求新快照。写操作不使用“丢弃旧请求”替代互斥。
5. 搜索高亮只处理原始文本，禁止在生成 HTML 后继续正则替换。日志搜索按字面匹配。
6. 发布必须跑新增写锁/快照/防抖/浏览器失败分支测试；通过自动化不等于三台设备升级后完成真机联网验证。
